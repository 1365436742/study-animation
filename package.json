{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "lerna run dev",
    "publish": "lerna publish -c",
    "ls": "lerna ls"
  },
  "devDependencies": {
    "lerna": "^5.6.2"
  }
}